plugins {
    id 'com.android.library'
    id 'kotlin-android'
}

android {
    namespace 'com.app.platform'
    compileSdk versions.compileSdk

    defaultConfig {
        minSdk versions.minSdk
    }
}

dependencies {
    api project(':navigation')
    implementation project(':impl')
}